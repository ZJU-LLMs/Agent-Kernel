# Core framework (includes fastapi, uvicorn, ray, pyyaml, json_repair, storages, etc.)
agentkernel-distributed[all]

# Additional dependencies not included in agentkernel-distributed
python-multipart  # Required for FastAPI file upload functionality
